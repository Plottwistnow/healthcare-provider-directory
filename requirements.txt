streamlit==1.20.0
pandas==1.5.3
requests==2.28.2
altair==4.2.2    # (optional, for possible charts)
numpy==1.23.5    # (if needed for numeric operations, optional)
geopy==2.3.0     # (optional, if using geopy for geocoding instead of manual requests)
